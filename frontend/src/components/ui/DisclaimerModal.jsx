/*
 * components/ui/DisclaimerModal.jsx
 *
 * What this is:
 *   A blocking modal overlay that fires when the user first lands on
 *   ModelsPage. The user must acknowledge the disclaimer before accessing
 *   the model selection content.
 *
 * Where it is used:
 *   ModelsPage.jsx only.
 *
 * What it should contain:
 *   - A semi-transparent blurred overlay covering the full screen
 *   - A centred card with the disclaimer text (not financial advice, for
 *     educational purposes only)
 *   - A single confirm button that closes the modal and reveals the page
 *   - Controlled by a useState boolean in ModelsPage (not internal state)
 *
 * Props:
 *   onConfirm  function called when the user clicks confirm
 *
 * Development order:
 *   Build when starting ModelsPage. It is the first thing the user sees
 *   on that route so it needs to work before the carousel is built.
 */


/*
* made as an individual component instead of in the models page
* this is a popup modal which will appear when the user visits the models page
* this is kept as an individual component
* it is currently being used in models pages 
* the onAgree function is passed from models page
* when user clicks button it tells models page the user has agreed
*/


export default function DisclaimerModal({ onAgree }) {
    return (

        // the dark overlay that covers the whole screen

    <div className="fixed inset-0 bg-black/50 z-[200] flex items-center justify-center p-6">
        
        {/* white box in the middle of the screen when clicking the models page */}
        <div className="bg-[var(--color-card-bg)] rounded-2xl max-w-lg w-full p-10">

            <p className="text-xs font-semibold tracking-widest text-[var(--color-muted)] uppercase mb-4">
                Important Notice
            </p>

            <h1 className="text-2xl font-bold text-[var(--color-ink)] leading-tight mb-6">
                This is an educational tool, not financial advice.
            </h1>

            <p className="text-[var(--color-muted)] text-base leading-relaxed mb-4">
                The predictions shown on this platform are generated by machine learning models trained on historical S&P 500 data. {" "}
                <strong className="text-[var(--color-ink)]">
                    Past performance does not guarantee future results.
                </strong>
            </p>

            <p className="text-[var(--color-muted)] text-base leading-relaxed mb-8">
                This system is built for academic and analytical purposes only. Do not use these predictions to make real investment or trading decisions. Markets are affected by events that no historical model can anticipate.
            </p>

            <hr className="border-[var(--color-border)] mb-6" />

            {/* the agree button when clicked it runs onAgree from the models pages */}
            <button
                onClick={onAgree}
                className="w-full bg-[var(--color-ink)] text-[var(--color-off-white)] py-4 rounded-xl text-base font-medium hover:opacity-75 transition-opacity"
            >
                I understand and agree &rarr;
            </button>
        </div>
    </div>
    );
}