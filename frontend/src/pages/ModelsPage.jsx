/*
 * pages/ModelsPage.jsx
 *
 * What this is:
 *   The model selection page where users browse all six ML models
 *   before choosing one to explore.
 *
 * Where it is used:
 *   App.jsx at route /models
 *
 * What it should contain:
 *   - DisclaimerModal fired on mount, blocks content until confirmed
 *   - Two column layout after confirmation
 *   - Left: live S&P index pill, about text, three-step how-it-works
 *   - Right: ModelCarousel with all six model cards
 *   - Bottom CTA linking to /evaluations
 *
 * State:
 *   disclaimerConfirmed   boolean, controls whether modal shows
 *
 * Development order:
 *   Build after HomePage. Depends on DisclaimerModal, ModelCarousel,
 *   ModelCard, and Sparkline all being ready first.
 */




// using useState will let us track whetehr the user has clicked agree or not 

/* the disclaimer popup (modal) 
* when the button is clicked it tells the models page that the user has agreed
* onAgree is the function that is passed in the models page
*/


import { useState } from "react";


function DisclaimerModal({ onAgree }) {
    return (

        // the dark overlay that covers the whole screen

    <div className="fixed inset-0 bg-black/50 z-[200] flex items-center justify-center p-6">
        
        {/* white box in the middle of the screen when clicking the models page */}
        <div className="bg-white rounded-2xl max-w-lg w-full p-10">

            <p className="text-xs font-semibold tracking-widest text-gray-500 uppercase mb-4">
                Important Notice
            </p>

            <h1 className="text-2xl font-bold text-black leading-tight mb-6">
                This is an educational tool, not financial advice.
            </h1>

            <p className="text-gray-600 text-base leading-relaxed mb-4">
                The predictions shown on this platform are generated by machine learning models trained on historical S&P 500 data. {" "}
                <strong className="text-black">
                    Past performance does not guarantee future results.
                </strong>
            </p>

            <p className="text-gray-600 text-base leading-relaxed mb-8">
                This system is built for academic and analytical purposes only. Do not use these predictions to make real investment or trading decisions. Markets are affected by events that no historical model can anticipate.
            </p>

            <hr className="border-gray-200 mb-6" />

            {/* the agree button when clicked it runs onAgree from the models pages */}
            <button
                onClick={onAgree}
                className="w-full bg-black text-white py-4 rounded-xl text-base font-medium hover:bg-gray-800 transition-colors"
            >
                I understand and agree &rarr;
            </button>
        </div>
    </div>
    );
}

export default function ModelsPage() {

    //so false means modal (pop-up) shows and true means modal hidden
    //will start as false so the modal pops up straight away when you click the models page
    const [disclaimerConfirmed, setDisclaimerConfirmed] = useState(false);

    return (    
        <div>

            {/* the ! means if not confirmed then show the modal */}
            {/* when the use clicks agree the disclaimerConfirmed is now true and the modal disappears */}
            {!disclaimerConfirmed && ( 
                <DisclaimerModal onAgree={() => setDisclaimerConfirmed(true)} />
            )}

            {/* so I guess the rest of the page content goes here later??!! */}
            <div></div>
        </div>
    );
}